package obligatorio.obli.models.Usuarios;

public class Administrador extends User {
  public Administrador(int id, String ci, String nombre, String password) {
    super(id, ci, nombre, password);
  }
}
